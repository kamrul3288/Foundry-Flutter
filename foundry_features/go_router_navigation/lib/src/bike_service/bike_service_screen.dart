import 'package:flutter/material.dart';

class BikeServiceScreen extends StatelessWidget {
  const BikeServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bike Service')),
      body: const Center(child: Text('Bike Service')),
    );
  }
}
