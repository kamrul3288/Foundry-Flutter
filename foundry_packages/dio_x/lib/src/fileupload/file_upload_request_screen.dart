import 'package:flutter/material.dart';

class FileUploadRequestScreen extends StatelessWidget {
  const FileUploadRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('File Upload Request')),
      body: const Center(child: Text('File Upload Request')),
    );
  }
}
