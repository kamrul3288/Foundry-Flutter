import 'package:flutter/material.dart';

class AppCheckboxColors {
  final Color checked;
  final Color unchecked;
  final Color disabled;
  final Color error;

  const AppCheckboxColors({
    required this.checked,
    required this.unchecked,
    required this.disabled,
    required this.error,
  });
}
