enum AppCardTone { warning }

enum AppCardVariant { filled, outline, filledOutline }

enum AppCardCornerRadius { sm, md, lg }

enum AppCardCornerMode { all, top, bottom, left, right }

enum AppCardShape { rounded, pill, circle }
