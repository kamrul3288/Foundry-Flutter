// The visual intent of a radio group.
/// [brand] uses the brand/primary color to highlight the selected state.
/// low-emphasis contexts where brand color would feel too loud.
enum AppRadioIntent { brand }
